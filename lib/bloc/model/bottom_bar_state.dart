import 'package:flutter/material.dart';

class BottomBarState {
  BottomBarState(
      {this.callsColor, this.chatsColor, this.friendsColor, this.profileColor});
  Color? callsColor;
  Color? chatsColor;
  Color? friendsColor;
  Color? profileColor;
}
